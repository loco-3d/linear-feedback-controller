passthrough_controller:
  reference_interfaces: {
    type: string_array,
    description: "Names of the interfaces to be forwarded",
    validation: {
          not_empty<>: null,
          unique<>: null,
        }
  }
  command_interfaces: {
    type: string_array,
    description: "Names of the interfaces to forward to.",
    validation: {
          not_empty<>: null,
          unique<>: null,
        }
  }
